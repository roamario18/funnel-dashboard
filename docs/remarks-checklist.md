# Чек-лист замечаний

Замечания **от 11.09.2026** вынесены в [remarks-11-09-2026.md](remarks-11-09-2026.md). 

**Как открывать приложение:** портал `https://bs18.bitrix24.ru` → левое меню **«Дашборд воронки продаж»** / **«Воронка продаж»**. Прямая ссылка Black Hole (`app-….vibecode.bitrix24.tech`) даёт 401 — так и задумано.

**Живой URL контейнера (только из меню):** https://app-7a4dbac2cc1e.vibecode.bitrix24.tech  
**Исходники:** https://github.com/roamario18/funnel-dashboard  
**Срез ключа без секрета:** [docs/me.json](me.json)

Доступ для проверки на портале передаётся в переписке по тикету. 

| № | Тема | Статус |
|---|---|---|
| 15.09.1 | Аналитика через `POST /v1/deals/aggregate` по валюте, список сделок `limit: 20` | закрыто |
| 15.09.2 | Снять `entity` с ключа и обновить `docs/me.json` | закрыто |
| 15.09.3 | Демо-доступ или скрины «смешанные валюты» и «пустой период» | закрыто учётом на портале |
| 15.09.4 | Таймаут шлюза Black Hole и скорость загрузки | закрыто |
| 1 | `READONLY` + файл `GET /v1/me` | закрыто |
| 2 | Боевой ключ без `vibe:infra`, только `crm, user, placement` | закрыто, см. [remarks-11-09-2026.md](remarks-11-09-2026.md) |
| 3 | `invalid_state` на одноразовом `code` | закрыто |
| 4 | Предупреждение о смешении валют | закрыто, усилено |
| 5 | Фолбэк `/users/:id` на 401/403 | закрыто |
| 6 | README: доступы и отказ по правам | закрыто |
| А | Фильтр 7/30 дней отбрасывал сделки за сегодня | закрыто |
| Б | KPI считали $ как рубли | закрыто |
| В | Суммы округлялись до целых (73 вместо 73,20) | закрыто |

---

## 15.09.1. Вернуть аналитику на `POST /v1/deals/aggregate`

**Проблема.** Считать KPI и стадии через `POST /v1/deals/aggregate`: по одному вызову на валюту (`filter.currency`), `groupBy: stageId`; пересчитать по курсу CRM и сложить. Список последних сделок снова грузить с `limit: 20`.

**Что сделано.** KPI (сумма открытых, число выигранных, средний чек) и таблица стадий считаются агрегатами, не списком сделок. Для каждой валюты в периоде — `POST /v1/deals/aggregate` с `filter.currency` и `groupBy: stageId`. Суммы переводятся в рубли по курсу CRM и складываются. `POST /v1/deals/search` для таблицы — `limit: 20`. Имя поля фильтра при необходимости пробуется один раз (`currency` → проба `currencyId` на одной валюте), без второго полного круга по всем кодам.

---

## 15.09.2. Снять скоуп `entity`

**Проблема.** Убрать неиспользуемый скоуп `entity` в настройках ключа и обновить `docs/me.json`.

**Что сделано.** Ключ перевыпущен без `entity`. В [docs/me.json](me.json) скоупы `crm`, `user`, `placement` плюс платформенные `vibe:ai` и `vibe:search` (снимаются в карточке, но платформа оставляет их на `vibe_app_`). Дашборд `entity` не вызывает.

---

## 15.09.3. Скрины смешанных валют и пустого периода

**Проблема.** Передать доступ на демо-портал либо приложить скриншоты «данные есть (со смешанными валютами)» и «пустой период».

**Что сделано.** Учётка для проверки на портале передаётся в переписке по тикету. Смешанные валюты и пустой период смотрятся на живом дашборде.

---

## 15.09.4. Таймаут шлюза и скорость загрузки

**Проблема.** Шлюз Black Hole обрывает HTML и `/api/dashboard`, если ответ приложения дольше лимита (`The gateway did not get a response in time`). Лишние круги `deals/aggregate` (все валюты с `currency`, затем все с `currencyId`) и последовательный `users/search` после агрегатов раздували время. В UI гонка запросов давала ложное «Сервер приложения недоступен».

**Что сделано.**
- `GET /` при placement (`code` без `state`) обменивает код за 8 с и одну попытку — страница меню не должна упираться в шлюз.
- Агрегаты: таймаут 20 с, без ретрая; один `groupBy currency`; параллельные вызовы по валютам; `currencyId` — только проба на одной валюте, если первая пачка пустая.
- Справочники воронок, стадий и курсов — кэш в памяти процесса на 45 секунд (по сессии). KPI и список сделок не кэшируются.
- ФИО ответственных запрашиваются сразу после `deals/search`, параллельно с агрегатами.
- Баннер «Считаем суммы и стадии…»; `/api/me` и дашборд грузятся независимо; ошибка шлюза переводится на русский.

Пока крутится баннер, период лучше не переключать повторно.

---

## 1. Ключ в режиме READONLY и вывод `/v1/me`

**Проблема.** Перевести `vibe_app_` в `READONLY` после разовой привязки меню и приложить `GET /v1/me` без самого ключа: `accessMode: "READONLY"` и фактические скоупы.

**Что сделано.** Ключ приложения переведён в «Только чтение». В [docs/me.json](me.json) — `"accessMode": "READONLY"` и фактические скоупы, без секрета. Повторный деплой этим ключом недоступен (`WRITE_BLOCKED_READONLY_KEY`). Режим `READWRITE` держали только пока создавали сервер и биндили меню.

---

## 2. Убрать `vibe:infra` с боевого ключа

**Проблема.** Деплоить личным `vibe_api_` либо выпустить приложение со скоупами только `crm`, `user`, `placement`.

**Что сделано.** С боевого ключа снят `vibe:infra`. Фактические скоупы данных: `crm`, `user`, `placement`. Платформа оставляет `vibe:ai` и `vibe:search` на ключе авторизации; в `READONLY` эти вызовы недоступны. Подробнее — [remarks-11-09-2026.md](remarks-11-09-2026.md) п. 2.

---

## 3. Ветка одноразового `code` → `invalid_state`

**Проблема.** Починить или удалить приём кода в `server.js` — placement отдаёт `/?code=…` без `state`, редирект на `/auth/callback` всегда давал `invalid_state`.

**Что сделано.** Код без `state` обменивается на сессию через `POST /v1/oauth/token` (`redirect_uri` = origin приложения), ставится cookie, редирект на `/`. На `/auth/callback` уходит только пара `code` + `state` (ручной «Войти через Битрикс24»). Открытие из левого меню больше не заканчивается `invalid_state`.

---

## 4. Смешение валют

**Проблема.** Показать смешение: агрегация по валюте или строка в `warnings`, если валют больше одной. KPI и стадии складывали числа как есть — 73 $ выглядели как 73 ₽.

**Что сделано.** При нескольких валютах показывается предупреждение. KPI и суммы стадий пересчитываются в рубли по курсу CRM (`GET /v1/currencies`). В таблице последних сделок сумма остаётся в валюте сделки. Курс берётся из справочника портала, не с биржи.

---

## 5. Поштучный фолбэк пользователей на 401/403

**Проблема.** Не вызывать фолбэк `GET /users/:id` на 401/403, только сеть и 5xx.

**Что сделано.** При отказе `users/search` с 401/403/4xx в таблице остаётся `ID`, второго запроса нет. Поштучный `GET /users/:id` — только при сбое сети или 5xx. Сотрудник с правом `user` видит ФИО.

---

## 6. README: доступы

**Проблема.** Демо-доступ или три состояния UI (данные / пустой период / отказ по правам); дописать оборванный пункт про доступы в «Ограничениях».

**Что сделано.** В README дописаны ограничения, актуальный URL контейнера и ссылка на этот чек-лист. Учётка для проверки на портале передаётся в переписке по тикету. На портале проверяются три состояния: данные, пустой период, отказ по правам CRM.

---

## А. Фильтр 7 / 30 дней прятал сегодняшние сделки

**Проблема.** Верхняя граница периода по `createdAt` обрезалась до полуночи UTC текущей даты, поэтому сделки, созданные сегодня после полуночи UTC, не попадали в фильтры «7 дней» и «30 дней».

**Что сделано.** Верхняя граница периода — начало следующих суток UTC (`utcTomorrowIso`). Сделки за сегодня входят в выборку 7 и 30 дней.

---

## Б. 73 $ в KPI как 73 ₽

**Проблема.** Развитие пункта 4: агрегат `sum(amount)` игнорировал валюту, долларовая сумма попадала в KPI как рубли.

**Что сделано.** Суммы открытых, средний чек и стадии считаются в рублях с пересчётом по курсу CRM. В таблице строка остаётся в исходной валюте (например 73,20 $). Над таблицей — предупреждение с использованным курсом.

---

## В. Округление сумм до целых

**Проблема.** Суммы отображались без копеек: 73,20 выглядело как 73.

**Что сделано.** Деньги (KPI, стадии, таблица) выводятся с двумя знаками после запятой. Число выигранных сделок по-прежнему целое.
